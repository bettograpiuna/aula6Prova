# Projeto: Aula 6
<a href="https://bettograpiuna.github.io/aula6Prova/" target="_blank">Testar Projeto - Acessar</a>

## Sobre o projeto

Projeto simples para demonstração de layout e interação; objetivo principal: facilitar testes de usabilidade e validação rápida de mudanças.

## 🧩 Programa: Gerenciador de Tarefas 

Resumo: exemplo simples que demonstra o uso de funções anônimas, arrow functions e callbacks para gerenciar uma lista de tarefas. O código de exemplo pode ser colocado em `script.js`.

- **Adicionar (função anônima):** atribuir uma função anônima a uma variável para inserir uma nova tarefa no array.
- **Listar (arrow function):** usar uma arrow function para exibir cada tarefa com seu índice no console.
- **Operações (callback):** criar uma função que recebe um callback para permitir remover, atualizar ou marcar uma tarefa como concluída.
- **Interação:** usar `prompt()` para receber ações do usuário e `console.log()` para mostrar resultados.

Fluxo resumido: usuário escolhe ação via `prompt` → função correspondente executa (callback) → resultado exibido com `console.log`.

---

## ✅ Link de Teste

Clique no botão acima para abrir o site do projeto em uma nova aba. Ou a alternativa abaixo.

> Alternativa: link direto — [https://bettograpiuna.github.io/aula6Prova/]

---

## ✨ Recursos

- Layout responsivo (desktop, tablet e mobile)
- Arquivos principais: `index.html`, `script.js`, `style.css`
- Pronto para testes de usabilidade

---

## 🛠️ Tecnologias

- HTML5
- CSS3 (flexbox / grid)
- JavaScript (vanilla)

---

## ▶️ Como testar localmente

1. Clone ou baixe este repositório.
2. Abra a pasta do projeto.
3. Abra o arquivo `index.html` no navegador (duplo-clique ou arraste para o navegador).

Opcional (servidor local):

```
# via Python 3 (na pasta do projeto)
python -m http.server 8000
# então abra http://localhost:8000
```

---

## 📁 Estrutura do projeto

- `index.html` — página principal
- `style.css` — estilos
- `script.js` — interatividade

---

## 🤝 Contribuição

1. Abra uma issue descrevendo a sugestão ou bug.
2. Abra um pull request com mudanças pequenas e explicativas.
